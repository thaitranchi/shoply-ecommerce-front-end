// src/reportWebVitals.js

const reportWebVitals = (metric) => {
    console.log(metric);  // You can log the metrics to the console or send them to an analytics endpoint
  };
  
  export default reportWebVitals;
  